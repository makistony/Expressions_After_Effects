// Put the code on the Orientation of camera

targL = thisComp.layer("CamTarget"); // Create a null called "CamTarget"
targLWSpace = targL.toWorld([0,0,0]);
targLCamSpace = thisLayer.fromWorld(targLWSpace);

lookAt([0,0,0], targLCamSpace);
